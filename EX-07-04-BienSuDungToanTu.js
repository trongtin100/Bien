tinWidth = prompt ("Enter the width");
tinHeight= prompt ("Enter the height");

let width = parseInt(tinWidth);
let height = parseInt(tinHeight);
let area = width * height ;
    document.write("The area is: " + area )